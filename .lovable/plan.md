

## Plan: Update Cessna 152 Hourly Rate

### What needs to change
Update the hourly rate for the Cessna 152 (N46826) from `$120/Hour` to `$140/Hour` in the Fleet component.

### File to modify
- `src/components/Fleet.tsx` - Line 34: Change `hourlyRate: '$120/Hour'` to `hourlyRate: '$140/Hour'`

This is a single-line change to update the pricing for the first aircraft in the fleet array.

